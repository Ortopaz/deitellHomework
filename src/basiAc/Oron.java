package basiAc;


    public class Oron {
        public int getTemperature;
        public boolean toCheckIfAcCanSwitchOff;
        private boolean remote;
        private int temperature;

        public void setSwitch() {
        if (remote) {
            remote = false;
        }
        else {remote = true;
        }

        public int clalculatorQuoatientIdentor(int firstnumber, secondnumber)


    }
    public boolean isOn() {
        return remote;
    }
        public boolean isOff() {
            return remote;
        }
        public void increaseTemperature(  ) {

            if  (temperature < 30) {temperature = temperature + 1;}
            if (temperature > 30) {System.out.print ("Temperature will not go beyond 30");}
        }

        public int getTemperature() {
            return temperature;

        }

        public void decreaseTemperature() {
            temperature = temperature -1;
        }
    }
